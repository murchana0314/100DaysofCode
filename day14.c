//Write a program to print the sum of the first n odd numbers.

/*#include<stdio.h>

int main()
{
    int n ,sum = 0 ,count = 1;
    printf("Enter the number of odd numbers you want to find the sum of: ");
    scanf("%d",&n);

    for(int i = 1; count <= n; i = i + 2)
    {
    sum = sum + i;
    count++;
    }
    printf("The sum of first n odd numbers is %d",sum);

    return 0;
}*/

//Write a program to print the product of even numbers from 1 to n.

/*#include<stdio.h>

int main()
{
    int n ,prod = 1 ,count = 1;
    printf("Enter the number of even numbers you want to find the product of: ");
    scanf("%d",&n);

    for(int i = 2; count <= n; i = i + 2)
    {
    prod = prod*i;
    count++;
    }
    printf("The product of first even numbers is %d",prod);

    return 0;
}*/